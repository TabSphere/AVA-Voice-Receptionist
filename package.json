{
  "name": "ava-voice-receptionist-dashboard",
  "version": "1.0.0",
  "description": "AVA Voice Receptionist Dashboard — Twilio voice engine + React admin UI for TabSphere Limited",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "concurrently -n server,client -c yellow,cyan \"node --watch src/server.js\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "NODE_ENV=production node src/server.js",
    "test": "node test/smoke.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "nodemailer": "^6.9.14",
    "openai": "^4.60.0",
    "twilio": "^5.3.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
